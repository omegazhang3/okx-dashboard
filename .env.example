# OKX Dashboard Environment Variables
# Copy this file to .env.local and fill in your values

# Dashboard login credentials (required)
DASHBOARD_USER=your_username
DASHBOARD_PASS=your_password

# OKX CLI home directory (optional, defaults to $HOME)
# OKX_HOME=/opt/data/home
